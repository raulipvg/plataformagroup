import { ClienteTelefonoPipe } from './cliente-telefono.pipe';

describe('ClienteTelefonoPipe', () => {
  it('create an instance', () => {
    const pipe = new ClienteTelefonoPipe();
    expect(pipe).toBeTruthy();
  });
});
